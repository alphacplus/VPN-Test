# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with 'go test -c'
*.test

# Output of the go coverage tool, specifically when used with Litmus
*.out

# Dependency directories (remove the // [node_modules] once you decide to check in your vendor folder)
vendor/

# Go workspace file
go.work
go.work.sum

# env files
.env
schema.sql
.gitignore
README-GROUPS.md
